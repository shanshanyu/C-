int power1(int base, int n) {
	int f = 1;
	while (n-- > 0) f = f * base;
	return f;
}

int power(int base, int n) {
  if(n==0) return 1;
	else if (n == 1) return base;
	else if (n > 1) return base * power(base, n - 1);
}


利用for 循环，while循环   函数递归 实现都可以
